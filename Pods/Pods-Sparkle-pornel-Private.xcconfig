#include "Pods-Sparkle-pornel.xcconfig"
FRAMEWORK_SEARCH_PATHS = ${PODS_SPARKLE_PORNEL_FRAMEWORK_SEARCH_PATHS}
GCC_PREPROCESSOR_DEFINITIONS = COCOAPODS=1
HEADER_SEARCH_PATHS = "${PODS_ROOT}/BuildHeaders" "${PODS_ROOT}/BuildHeaders/Sparkle-pornel" "${PODS_ROOT}/Headers" "${PODS_ROOT}/Headers/Sparkle-pornel"
OTHER_LDFLAGS = -ObjC ${PODS_SPARKLE_PORNEL_OTHER_LDFLAGS}
PODS_ROOT = ${SRCROOT}