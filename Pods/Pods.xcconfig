FRAMEWORK_SEARCH_PATHS = "${PODS_ROOT}/Sparkle-pornel" "$(PODS_ROOT)/Sparkle-pornel"
GCC_PREPROCESSOR_DEFINITIONS = $(inherited) COCOAPODS=1
HEADER_SEARCH_PATHS = "${PODS_ROOT}/Headers" "${PODS_ROOT}/Headers/Sparkle-pornel"
OTHER_CFLAGS = $(inherited) -isystem "${PODS_ROOT}/Headers" -isystem "${PODS_ROOT}/Headers/Sparkle-pornel"
OTHER_LDFLAGS = -ObjC -framework Sparkle
PODS_ROOT = ${SRCROOT}/Pods